name: Myblog to Github Repo
#hahaha
on: push

jobs:
  git-mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: wearerequired/git-mirror-action@v1
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        with:
          source-repo: 'git@gitee.com:xsjx/typecho-theme-MDUI2333.git'
          destination-repo: 'git@github.com:222e/theme.git'
